<div>
    <h1>
        {{name}}'s page
    </h1>
    <div>
        <h2>
            Wishlist:
        </h2>
        <ul>
            {{#each items as |item index|}}

             <li>{{item.name}}</li>

            {{/each}}
        </ul>
    </div>
</div>