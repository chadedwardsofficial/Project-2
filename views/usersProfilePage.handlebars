<div class="wishlist">
    <h1 class="userPageName">
        {{name}}'s page
    </h1>

    
    <div class="list">
        <h2>
            Wishlist:
        </h2>
        <ul>
            {{#each items as |item index|}}

             <li> {{item.name}}</li>

            {{/each}}
        </ul>
    </div>
</div>